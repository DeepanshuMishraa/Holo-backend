{
  "name": "be",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "postinstall": "bunx prisma generate",
    "start": "bun run src/index.ts",
    "build": "bunx esbuild --bundle src/index.ts --format=esm --outfile=dist/_worker.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@kinde-oss/kinde-typescript-sdk": "^2.9.1",
    "@prisma/client": "^6.3.1",
    "hono": "^4.7.0",
    "prisma": "^6.3.1"
  }
}
